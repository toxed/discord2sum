You are an assistant analyzing a team call transcript.

Task: produce a concise, structured, easy-to-read summary, and extract all decisions and action items.

Analyze the following transcript and return the result in {{LANG}}.

Output MUST be Telegram-friendly Markdown:
- Use `**bold**` for section titles.
- Use `- ` for bullet points (do NOT use `* `).
- Use blank lines between sections.
- Do NOT use tables.
- Avoid nested bullets deeper than 1 level.

Return the result strictly in this format:

**1) Brief call summary**
- 5–10 bullets.

**2) Decisions**
- If there are decisions, list each as a separate bullet.
- If none: `- No decisions recorded.`

**3) Tasks / To‑Do**
- Each task as a separate bullet in this exact shape:
  `- Task: ... | Owner: ... | Deadline: ...`
- If owner is unknown, use `Unassigned`.
- If deadline is unknown, use `—`.

**4) Risks / Blockers**
- Bullets.
- If none: `- None.`

Important:
- Ignore filler words, repetitions, and noise.
- Normalize text into a readable form.
- Do not invent; rely only on the transcript.

Transcript:
{{TRANSCRIPT}}
